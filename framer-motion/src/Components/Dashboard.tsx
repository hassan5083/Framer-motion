import { Layout } from "./Layout";
import { Wrapper } from "../theme/styleComponents";

export const Dashboard = () => {
  return (
    <Layout>
      <Wrapper>Dashboard</Wrapper>
    </Layout>
  );
};
